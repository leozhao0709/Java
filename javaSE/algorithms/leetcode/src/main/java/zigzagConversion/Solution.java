package zigzagConversion;

class Solution {
    public String convert(String s, int numRows) {
        if (s.length() <= numRows) {
            return s;
        }

        char[][] chars = char[numRows][]
    }
}
